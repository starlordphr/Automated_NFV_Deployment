This project aims to deploy NFV components automatically on different VMs using a simple SLA format.

HOW TO USE IT:
cd into ui directory.
run the script as sudo, using the command:
sudo python ui.py

Functionalities
1. Deploy VM (Usage: deploy sla.conf)
2. Check VMs status (Usage: list server)
3. Delete VMs (Usage: delete --all or delete --instance instance_name)
4. Get some help to connect to the VM (Usage: list ip)
